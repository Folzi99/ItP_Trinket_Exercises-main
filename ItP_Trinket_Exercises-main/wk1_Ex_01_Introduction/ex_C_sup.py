# Exercise C Write a Python program to compute the area of a rectangle. Prompt the user to enter the width and height of the rectangle. Print a nice message with the answer.

legth = input('Enter length: ')
width = input('Enter width: ')
area = float(legth) * float (width)

print(f'The are of the square is {round(area,2)}. \nBye!')
